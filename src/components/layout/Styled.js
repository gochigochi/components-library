import styled from "styled-components"

export const Layout = styled.div`
    width: 95%;
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 5rem;
    padding: 4rem 0;
`